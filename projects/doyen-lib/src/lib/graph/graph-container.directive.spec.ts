import { GraphContainerDirective } from './graph-container.directive';

describe('GraphContainerDirective', () => {
  it('should create an instance', () => {
    //    const directive = new GraphContainerDirective();
    //    expect(directive).toBeTruthy();
  });
});
